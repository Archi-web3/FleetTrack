const mongoose = require('mongoose');
const SecurityConfig = require('./models/security-config.model');
const Mouvement = require('./models/mouvement.model');
const Utilisateur = require('./models/utilisateur.model');

// Instead of connecting directly, we can use a local tool or check the models.
// Let's print the actual data. Wait, DNS is failing, so we cannot connect from here.
